hero_title:
  default: "Real alumni advice, actual results"
hero_subtitle:
  default: "Connect with alumni, explore popular courses, and attend curated events."
cta_primary_label:
  default: "Explore Discussions"
cta_primary_url:
  default: "/latest"
cta_secondary_label:
  default: "Join the Community"
cta_secondary_url:
  default: "/signup"
hero_stat_1:
  default: "4k+ Alumni"
hero_stat_2:
  default: "200+ Universities"
hero_stat_3:
  default: "12+ Years"
show_alumni_spotlight:
  type: bool
  default: true
alumni_endpoint:
  default: ""
show_most_talked:
  type: bool
  default: true
most_talked_endpoint:
  default: ""
show_latest:
  type: bool
  default: true
latest_endpoint:
  default: "/latest.json"
show_popular_courses:
  type: bool
  default: true
popular_courses_endpoint:
  default: ""
show_events:
  type: bool
  default: true
events_endpoint:
  default: ""
show_categories:
  type: bool
  default: true
categories_endpoint:
  default: "/categories.json"
